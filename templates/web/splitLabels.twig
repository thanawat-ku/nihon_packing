{% extends "layout/layout.twig" %}

{% block css %}
    {% webpack_entry_css 'layout/datatables' %}
{% endblock %}

{% block js %}
    {% webpack_entry_js 'layout/datatables' %}
    {% webpack_entry_js 'web/splitLabels' %}
{% endblock %}

{% block content %}
  <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <h2><i class="fas fa-dolly"></i> Split Label list</h2>
                <button type="button" id="addBt" class="btn btn-primary btn-sm" data-toggle="modal" data-target="#addLot"><i style="font-size:15px; width:20px;" class="fas fa-dolly"></i> Add Lot</button>      
                <hr>
                <table id="my-data-table" class="table table-striped table-bordered dt-responsive nowrap dataTable no-footer dtr-inline collapsed">
                    <thead>
                    <tr>
                        <th>Split Label No</th>
                        <th>Label NO</th>
                        <th>status</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for splitLabel in splitLabels %}                    
                    <tr>
                        <td>{{ splitLabel.splitLabel_no }}</td>
                        <td>{{ splitLabel.label_no}}</td>
                        <td>{{ splitLabel.status}}</td>
                        <td>
                        {% if splitLabel.status == "CREATED" %}
                        <button type="button" id="printBt" class="btn btn-info btn-sm" data-toggle="modal" data-target="#printLot" name="{{ splitLabel|json_encode }}"><i style="font-size:15px; width:20px;" class="fas fa-print"></i></button>
                        {% endif %}
                        <button type="button" id="editBt" class="btn btn-warning btn-sm" data-toggle="modal" data-target="#editLot" name="{{ splitLabel|json_encode }}"><i style="font-size:15px; width:20px;" class="fas fa-edit"></i></button>
                        <button type="button" id="deleteBt" class="btn btn-danger btn-sm" data-toggle="modal" data-target="#deleteLot" name="{{ splitLabel|json_encode }}"><i style="font-size:15px; width:20px;" class="fas fa-trash"></i></button>
                        {% if splitLabel.status != "CREATED" %}
                        <a href="label_splitLabel?id={{splitLabel.id}}"><button type="button" class="btn btn-success btn-sm" data-toggle="modal" ><i style="font-size:15px; width:20px;" class="fas fa-info-circle"></i></button></a> 
                        {% endif %}
                        </td>
                    </tr>
                    {% endfor %}
                    </tbody>
                    <tfoot></tfoot>
                </table>
                <p></p>
            </div>
        </div>
    </div>
<!-- Modal add splitLabel -->
<div class="modal fade" id="addLot">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <form id="form-addLot" action="add_splitLabel" method="post">
        <!-- Modal Header -->
        <div class="modal-header">
          <h4 class="modal-title">Add Lot</h4>
          <button type="button" class="close" data-dismiss="modal">&times;</button>
        </div>
        <!-- Modal body -->
        <div class="modal-body">          
          <div class="form-product">
            <input type="hidden" id="addLotID" name="id">
            <label for="splitLabelNo">Lot No</label>
            <input type="text" autocomplete="off" class="form-control" id="addLotNo" name="splitLabel_no" required>
          </div>                 
          <div class="form-product">
            <label for="email">Lot</label>
            <select class="form-control"  id="addLotID" name="product_id">
            {% for p in products %}
              <option value={{ p.id }}>({{p.product_code}}) {{ p.product_name}} </option>
            {% endfor %}
            </select>
          </div>           
          <div class="form-product">
            <label for="splitLabelNo">Quantity</label>
            <input type="text" autocomplete="off" class="form-control" id="addQuantity" name="quantity" required>
          </div> 
        </div>
        <div class="modal-footer">
          <button type="submit" class="btn btn-primary">Save</button>
          <button type="button" class="btn btn-danger" data-dismiss="modal">Cancel</button>
        </div>
      </form>
    </div>
  </div>
</div>

<!-- Modal edit splitLabel -->
<div class="modal fade" id="editLot">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <form id="form-editLot" action="edit_splitLabel" method="post">
        <!-- Modal Header -->
        <div class="modal-header">
          <h4 class="modal-title">Edit Lot</h4>
          <button type="button" class="close" data-dismiss="modal">&times;</button>
        </div>
        <!-- Modal body -->
        <div class="modal-body">          
          <div class="form-product">
            <input type="hidden" id="editLotID" name="id">
            <label for="splitLabelNo">Lot No</label>
            <input type="text" autocomplete="off" class="form-control" id="editLotNo" name="splitLabel_no" required>
          </div>                 
          <div class="form-product">
            <label for="email">Product</label>
            <select class="form-control"  id="editProductID" name="product_id">
            {% for p in products %}
              <option value={{ p.id }}>({{p.product_code}}) {{ p.product_name}} </option>
            {% endfor %}
            </select>
          </div>           
          <div class="form-product">
            <label for="splitLabelNo">Quantity</label>
            <input type="text" autocomplete="off" class="form-control" id="editQuantity" name="quantity" required>
          </div> 
        </div>
        <div class="modal-footer">
          <button type="submit" class="btn btn-primary">Save</button>
          <button type="button" class="btn btn-danger" data-dismiss="modal">Cancel</button>
        </div>
      </form>
    </div>
  </div>
</div>

 
<!-- Modal Delete splitLabel -->
<div class="modal fade" id="deleteLot">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <form id="form-deleteLot" action="delete_splitLabel" method="post">
        <!-- Modal Header -->
        <div class="modal-header">
          <h4 class="modal-title">Delete Lot</h4>
          <button type="button" class="close" data-dismiss="modal">&times;</button>
        </div>
        <!-- Modal body -->
        <div class="modal-body">          
          <div class="form-product">
            <input type="hidden" id="deleteLotID" name="id">
            <input type="hidden" id="deleteLotNoLabelStatus" name="status">
            <label for="splitLabelNo">Do you want to delete splitLabel: <span id="deleteLotNo"></span></label>
          </div>                 
        </div>
        <div class="modal-footer">
          <button type="submit" class="btn btn-danger">Delete</button>
          <button type="button" class="btn btn-info" data-dismiss="modal">Close</button>
        </div>
      </form>
    </div>
  </div>
</div>


{% endblock %}