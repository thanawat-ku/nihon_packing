{% extends "layout/layout.twig" %}

{% block css %}
    {% webpack_entry_css 'layout/datatables' %}
{% endblock %}

{% block js %}
    {% webpack_entry_js 'layout/datatables' %}
    {% webpack_entry_js 'web/merges' %} <!-- Focus that!!!! -->
    {% webpack_entry_js 'web/merge_detail' %}
{% endblock %}

{% block content %}
  <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <h2><i class="fas fa-folder-plus"></i>Merge labels status</h2>     
                <hr>
                <table id="my-data-table" class="table table-striped table-bordered dt-responsive nowrap dataTable no-footer dtr-inline collapsed">
                    <thead>
                    <tr>
                        <th>Merge pack no</th>
                        <th>Product code</th>
                        {# <th>Label type</th> #}
                        <th>Status</th>
                        <th>STD pack</th>
                        <th>Operation</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for merge in labels %}    <!-- Focus that!!!!!!!! -->             
                    <tr>
                        
                        <td>{{ merge.merge_no}}</td>  <!-- Focus that!!!!!!!! -->
                        <td>{{ merge.product_code }}</td>  <!-- Focus that!!!!!!!! -->
                        {# <td>{{ merge.label_type }}</td> #}
                        <td>{{ merge.merge_status }}</td>
                        <td>{{ merge.std_pack }}</td>
                        {# <td>{{ merge.id }}</td> #}
                        <td>
                        <a href="merge_detail?id={{merge.id}}"><button type="button" class="btn btn-success btn-sm" data-toggle="modal" ><i style="font-size:15px; width:20px;" class="fas fa-info-circle"></i></button></a> 
                        </td>
                    </tr>
                    {% endfor %}
                    </tbody>
                    <tfoot></tfoot>
                </table>
                <p></p>
            </div>
        </div>
    </div>


<!-- Modal merge label button -->
<div class="modal fade" id="detail_mergelabel">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <form id="form-detail_mergelabel" action="merge_detail" method="get">
        <!-- Modal Header -->

        <!-- 
        <div class="modal-header">
          <h4 class="modal-title">Add Lot</h4>
          <button type="button" class="close" data-dismiss="modal">&times;</button>
        </div>
        -->

        <!-- Modal body -->
        <!-- 

        <div class="modal-body">          
          <div class="form-product">
            <label for="lotNo">Lot No</label>
            <input type="text" autocomplete="off" class="form-control" id="addLotNo" name="lot_no" required>
          </div>                 
          <div class="form-product">
            <label for="email">Product</label>
            <select class="form-control"  id="addProductID" name="product_id">
            {% for p in products %}
              <option value={{ p.id }}>({{p.product_code}}) {{ p.product_name}} </option>
            {% endfor %}
            </select>
          </div>           
          <div class="form-product">
            <label for="lotNo">Quantity</label>
            <input type="text" autocomplete="off" class="form-control" id="addQuantity" name="quantity" required>
          </div> 
        </div>
        <div class="modal-footer">
          <button type="submit" class="btn btn-primary">Save</button>
          <button type="button" class="btn btn-danger" data-dismiss="modal">Cancel</button>
        </div> Modal body 
        
        -->
      </form>
    </div>
  </div>
</div>

{% endblock %}